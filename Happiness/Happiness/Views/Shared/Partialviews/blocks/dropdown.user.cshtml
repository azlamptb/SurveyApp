<div class="dropdown-menu pull-right dropdown-menu-scale">
 @* <a class="dropdown-item" ui-sref="app.inbox.list">
    <span>Inbox</span>
    <span class="label warn m-l-xs">3</span>
  </a>
  <a class="dropdown-item" ui-sref="app.page.profile">
    <span>Profile</span>
  </a>*@
  <a href="@Url.Action("Manage", "Account")" class="dropdown-item" ui-sref="app.page.setting">
    <span>Change Password</span>
    <span class="label primary m-l-xs"></span>
  </a>
  <div class="dropdown-divider"></div>
  <as class="dropdown-item" ui-sref="app.docs">
    Need help?
  </as>
    @if (Request.IsAuthenticated)
    {
        using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new { id = "logoutForm" })) {
            @Html.AntiForgeryToken()
            <a class="dropdown-item" ui-sref="access.signin" href="javascript:document.getElementById('logoutForm').submit()">Sign out</a>
        }
    }
    else
    {
        Response.Redirect("~/Account/LogIn");
    }
  
</div>
